# Progress — colorpicker-display

## Status
`in-progress`

## Steps
- [x] 1. Declare `xdotool` (desktop.lst) and `gpick` (extra.lst) with their comments.
- [x] 2. Write `dwm-colorpicker`; reuse colorgen.sh's magick/convert fallback.
- [x] 3. Write `dwm-display` — xrandr presets, autorandr profiles when present.
- [x] 4. Bind `super + c` and `super + d` in sxhkdrc (re-check both are free first).
- [x] 5. KEYBINDINGS.md entries for both.
- [x] 6. ROADMAP §3: mark the rows this Epic closed, correct the `xcolor` row.
- [x] 7. CLAUDE.md dwm/bin list; full suite + `tests/lint.sh --strict`.

## Deviations

## Blockers
